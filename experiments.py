import argparse
import torch
import model.models as module_arch
from parse_config import ConfigParser
from utils import plot_stroke


def main(config):
    logger = config.get_logger('experiments')

    # build model architecture
    model = config.init_obj('arch', module_arch)
    logger.info(model)

    # Loading the weights of the model
    logger.info('Loading checkpoint: {} ...'.format(config.resume))
    checkpoint = torch.load(config.resume)
    state_dict = checkpoint['state_dict']
    if config['n_gpu'] > 1:
        model = torch.nn.DataParallel(model)
    model.load_state_dict(state_dict)

    # prepare model for testing
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model.to(device)
    model.eval()

    with torch.no_grad():
        # Plot a sample generated by the model
        sampled_stroke = model.generate_sample()
        plot_stroke(sampled_stroke)


if __name__ == '__main__':
    args = argparse.ArgumentParser(description='Experiments for the Handwriting Model')
    args.add_argument('-c', '--config', default=None, type=str,
                      help='config file path (default: None)')
    args.add_argument('-r', '--resume', default=None, type=str,
                      help='path to latest checkpoint (default: None)')
    args.add_argument('-d', '--device', default=None, type=str,
                      help='indices of GPUs to enable (default: all)')

    config = ConfigParser.from_args(args)
    main(config)
